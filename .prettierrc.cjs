module.exports = {
  // 每行最大宽度
  printWidth: 120,
  // 使用空格缩进
  useTabs: false,
  // 缩进空格数
  tabWidth: 2,
  // 语句末尾加分号
  semi: true,
  // 使用单引号
  singleQuote: true,
  // 多行时尽可能打印尾随逗号 (例如，对象、数组最后一个元素后的逗号)
  trailingComma: 'es5',
  // 在对象字面量的括号之间打印空格
  bracketSpacing: true,
  // 将多行 HTML、JSX、Vue 等元素的 `>` 放在最后一行的末尾
  bracketSameLine: false,
  // 箭头函数参数只有一个时，始终添加括号
  arrowParens: 'always',
  // 换行符，'auto' 表示维持现有
  endOfLine: 'auto',
};
